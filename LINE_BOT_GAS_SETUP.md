# 📖 คู่มือติดตั้งและทดสอบ: Foreman Daily Report + GAS + LINE Bot (แบบ Messaging API อย่างเดียว)
### โครงการ: Projectmanager_site (ทางเลือก A: ง่าย รวดเร็ว ไม่ต้องใช้ LIFF)

คู่มือนี้สรุปขั้นตอนการติดตั้งและทดสอบแบบ **ง่ายและเร็วที่สุด** โดยใช้เพียง **Messaging API Channel เดียว** ไม่ต้องยุ่งเกี่ยวกับ LIFF:
- โฟร์แมนเปิดหน้าเว็บรายงานผ่านมือถือ (Chrome / Safari หรือลิงก์จากห้องแชต)
- ข้อมูลบันทึกลง Google Sheets และรูปภาพบันทึกลง Google Drive
- **LINE Bot ส่งการ์ด Flex Message สรุปผลงานแจ้งเตือนเข้าห้องแชตหรือกลุ่ม LINE ของโครงการทันที!**

---

## 🏗️ แผนผังการทำงาน (Workflow)

```
[ โฟร์แมนเปิดเว็บรายงานบนมือถือ ]
(เปิดผ่านลิงก์ในแชต, Chrome, Safari หรือ Rich Menu)
        │
        ▼
[ เลือกชื่อโฟร์แมน / บริษัท ] (เครื่องมือถือจะจำชื่อไว้ให้อัตโนมัติ)
        │
        ├─ 🌅 รอบเช้า: กรอกคนงานเข้า (+/-) + งานที่คาดการณ์วันนี้ + รูปแถว Safety
        └─ 🌆 รอบจบงาน: ดึงแผนงานเช้ามาเทียบผลงานจริง (%) + รูปผลงาน + ฝนตก
        │
        ▼ (กดปุ่ม "ส่งรายงานหน้างาน")
[ Google Apps Script (Web App) ]
        ├─────────────────────────────┬─────────────────────────────┐
        ▼                             ▼                             ▼
[ 1. บันทึก Google Sheets ]   [ 2. เซฟรูป Google Drive ]   [ 3. ยิง LINE Bot Push ]
 - ตาราง 'Daily_Reports'       - โฟลเดอร์                   - ส่ง Flex Message Card
 - ตาราง 'Tasks_Detail'          'Construction_Site_Photos'   เข้ากลุ่ม LINE โครงการทันที!
```

---

## 🛠️ สิ่งที่ต้องทำมีแค่ 3 ขั้นตอนเท่านั้น!

---

### ขั้นตอนที่ 1: ติดตั้ง Google Sheets และ Google Apps Script (GAS)

1. เปิดเบราว์เซอร์ไปที่ [Google Sheets](https://sheets.new) เพื่อสร้างสเปรดชีตใหม่
2. ตั้งชื่อไฟล์ เช่น **"รายงานประจำวันหน้างาน CPM Site"**
3. ไปที่เมนูด้านบน: **ส่วนขยาย (Extensions)** > **Apps Script**
4. ลบโค้ดเริ่มต้นในไฟล์ `Code.gs` ออกทั้งหมด
5. เปิดไฟล์ [`gas/Code.gs`](file:///c:/Users/Admin/Desktop/Project%20manager/Projectmanager_site/gas/Code.gs) ในโปรเจกต์นี้ คัดลอกโค้ดทั้งหมดแล้ววางลงไป
6. กดปุ่ม **บันทึก (Save / รูปแผ่นดิสก์)**
7. ทำการ Deploy เป็น Web App:
   - กดปุ่มสีน้ำเงินด้านบนขวา: **การทำให้ใช้งานได้ (Deploy)** > **รายการทำให้ใช้งานได้ใหม่ (New deployment)**
   - คลิกไอคอนรูปฟันเฟืองด้านซ้าย เลือกประเภทเป็น **เว็บแอปพลิเคชัน (Web app)**
   - ตั้งค่าดังนี้:
     - **คำอธิบาย (Description)**: `CPM Site Report API v1`
     - **ดำเนินการในฐานะ (Execute as)**: `ฉัน (Me - อีเมลของคุณ)`
     - **ใครมีสิทธิ์เข้าถึง (Who has access)**: **ทุกคน (Anyone)** 👈 *(สำคัญมาก!)*
   - กดปุ่ม **การทำให้ใช้งานได้ (Deploy)**
   - ระบบจะขอสิทธิ์เข้าถึง (Authorize Access):
     - เลือกบัญชี Google ของคุณ
     - หากขึ้นหน้าต่างเตือนสีเทา ให้คลิก **Advanced (ขั้นสูง)** > **Go to ... (unsafe)**
     - กด **Allow (อนุญาต)**
8. คัดลอก **URL เว็บแอปพลิเคชัน (Web App URL)** ที่ได้ (ขึ้นต้นด้วย `https://script.google.com/macros/s/.../exec`) เก็บไว้

---

### ขั้นตอนที่ 2: สร้าง LINE Bot (Messaging API) ใน LINE Developers

ขั้นตอนนี้ใช้เวลาเพียง 2 นาที:

1. เข้าสู่ระบบ [LINE Developers Console](https://developers.line.biz/) ด้วยบัญชี LINE ของคุณ
2. กดปุ่ม **Create a new provider** (เช่น ตั้งชื่อว่า `CPM Construction`)
3. ในหน้า Provider กด **Create a Messaging API channel**:
   - **Channel name**: `CPM Site Assistant` (หรือชื่อบอทที่ต้องการ)
   - **Channel description**: `บอทแจ้งเตือนรายงานหน้างานประจำวัน`
   - **Category**: `Home/Civil engineering/Construction`
   - **Email**: ใส่อีเมลของคุณ
   - ติ๊กยอมรับเงื่อนไข แล้วกด **Create**
4. เมื่อสร้างเสร็จ ให้คลิกเข้าไปที่แท็บ **Messaging API**:
   - เลื่อนลงมาด้านล่างสุดที่หัวข้อ **Channel access token (long-lived)**
   - กดปุ่ม **Issue**
   - คัดลอก **Token ยาวๆ** ที่ระบบสร้างให้เก็บไว้
5. *(ทางเลือก)* สแกน QR Code ของบอทในแท็บ Messaging API เพื่อเพิ่มเพื่อนกับบอท หรือดึงบอทเข้ากลุ่ม LINE ของโครงการ

---

### ขั้นตอนที่ 3: นำ Token ไปใส่ใน Google Apps Script

1. กลับไปที่หน้า Google Apps Script ในแท็บเบราว์เซอร์ของ Google Sheets
2. ที่แถบเมนูด้านซ้ายสุด คลิกไอคอนรูปฟันเฟือง **การตั้งค่าโครงการ (Project Settings)**
3. เลื่อนลงมาที่หัวข้อ **คุณสมบัติของสคริปต์ (Script Properties)**
4. กดปุ่ม **เพิ่มคุณสมบัติของสคริปต์ (Add script property)**:
   - **คุณสมบัติ (Property)**: `LINE_CHANNEL_ACCESS_TOKEN`
   - **ค่า (Value)**: วาง Token ที่คัดลอกมาจากขั้นตอนที่ 2
5. *(ถ้าต้องการให้บอทยิงเข้ากลุ่ม LINE โดยตรง)* กดเพิ่มอีก 1 แถว:
   - **คุณสมบัติ (Property)**: `LINE_TARGET_ID`
   - **ค่า (Value)**: ใส่ Group ID (ขึ้นต้นด้วย `C...`) หรือ User ID ของคุณ (ขึ้นต้นด้วย `U...`)
   *(ถ้ายังไม่มี Group ID ระบบจะใช้ User ID จากหน้าเว็บเป็นค่าเริ่มต้นให้อัตโนมัติ)*
6. กด **บันทึกคุณสมบัติของสคริปต์ (Save script properties)**

---

### ขั้นตอนที่ 4: เปิดใช้งาน Webhook (เพื่อดึง UID และส่งลิงก์เข้าเว็บอัตโนมัติ)

เมื่อตั้งค่าขั้นตอนนี้ เมื่อช่างหรือโฟร์แมนทักแชตหาบอท (หรือกดปุ่มใน Rich Menu) บอทจะดึง UID และชื่อของช่าง แล้วตอบกลับด้วยปุ่มเข้าเว็บที่ผูก UID อัตโนมัติทันที:

1. ในหน้า **LINE Developers Console** > เข้า Channel บอทของคุณ > ไปที่แท็บ **Messaging API**
2. เลื่อนมาที่หัวข้อ **Webhook settings**:
   - กดปุ่ม **Edit** ในช่อง **Webhook URL**
   - นำ **Web App URL ของ Google Apps Script** (ที่ได้จากขั้นตอนที่ 1) มาวาง
   - กดปุ่ม **Update**
   - สลับสวิตช์ **Use webhook** ให้เป็น **ON (สีเขียว)** 👈 *(สำคัญมาก)*
3. เลื่อนลงมาที่หัวข้อ **LINE Official Account features**:
   - ที่หัวข้อ **Auto-reply messages** กดคลิก **Edit**
   - จะเปิดหน้าต่าง LINE OA Manager ให้เลือก **Response mode: Bot** และปิด **Auto-response: Disabled** เพื่อไม่ให้ระบบตอบกลับอัตโนมัติเริ่มต้นของ LINE ส่งข้อความซ้ำซ้อน
4. *(ทางเลือก)* ใน Google Apps Script หากต้องการเปลี่ยนโดเมนหน้าเว็บที่บอทจะส่งลิงก์ไป:
   - ไปที่ **Project Settings (รูปฟันเฟือง)** > **Script Properties**
   - กดเพิ่ม Property: `FRONTEND_WEB_URL`
   - Value: ใส่ URL เว็บรายงานจริงของคุณ (เช่น `https://your-domain.web.app` หรือค่าเริ่มต้น `http://localhost:8081`)

---

## 🧪 การเปิดทดสอบ (Test Run)

1. ในเครื่องคอมพิวเตอร์ ดับเบิลคลิกไฟล์ [`run_server.bat`](file:///c:/Users/Admin/Desktop/Projectmanager_site/run_server.bat)
2. เปิดเบราว์เซอร์ไปที่: **`http://localhost:8081`**
3. กดปุ่ม **⚙️ ตั้งค่า GAS**:
   - นำ Web App URL จากขั้นตอนที่ 1 มาวาง
   - กดปุ่ม **⚡ ทดสอบการเชื่อมต่อ (Ping)** เพื่อตรวจดูสถานะสีเขียว
   - กด **บันทึก URL**
4. **ทดสอบรอบเช้า (เปิดงาน)**:
   - เลือกแท็บ **🌅 เปิดงานตอนเช้า**
   - ปรับยอดกำลังพล (+/-)
   - กด **`➕ กดเพิ่มงานที่คาดการณ์วันนี้`** ใส่ชื่องานและเป้าหมาย
   - กด **+ รูปแถว Safety เช้า**
   - กดปุ่ม **🌅 ส่งรายงานเปิดงานตอนเช้า**
5. **ทดสอบรอบเย็น (รายงานจบงาน)**:
   - เลือกแท็บ **🌆 รายงานจบงาน**
   - สังเกตว่าระบบจะดึงแผนงานจากรอบเช้าขึ้นมาให้อัตโนมัติ!
   - ปรับ % ผลงานจริงที่ทำได้ และปริมาณจริง
   - กด **+ รูปผลงานหน้างาน**
   - กดปุ่ม **🌆 ส่งรายงานสรุปจบงานประจำวัน**
6. **ตรวจสอบผลลัพธ์**:
   - ใน **Google Sheets**: มีแถวสรุปแยกตามรอบเช้า/จบงาน พร้อมรายการงานย่อยในชีต `Tasks_Detail` และลิงก์รูปภาพใน Google Drive
   - ใน **LINE**: LINE Bot จะยิงการ์ด Flex Message แจ้งเตือนเข้าแชตทันที! 🎉

---

## 💬 คำสั่งพิมพ์ในห้องแชท LINE (Chat Commands)

สมาชิกในห้องแชตส่วนตัวหรือกลุ่ม LINE ของโครงการสามารถพิมพ์คำสั่งต่อไปนี้เพื่อเรียกใช้งานบอทได้ทันที:

| คำสั่งพิมพ์ในแชท | รายละเอียดการทำงาน | หน้าเว็บที่เชื่อมโยง |
|---|---|---|
| **`คำสั่ง`** หรือ **`help`** / **`เมนู`** | 🤖 **เปิดคู่มือคำสั่งทั้งหมด**: บอทจะตอบกลับด้วย Flex Card สรุปคำสั่ง และมีปุ่มลัดเปิดระบบทั้ง 3 ส่วน พร้อมแถบ Quick Reply ลัดด้านล่าง | แสดงเมนูรวม |
| **`รายงาน`** หรือ **`เปิดงาน`** / **`จบงาน`** | 📱 **เปิดระบบรายงานประจำวัน (โฟร์แมน - LV1)**: สำหรับรายงานคนงานเข้างาน, งานคาดการณ์เช้า, สรุปผลงานเย็น, เครื่องจักร, ปัญหา และภาพถ่ายหน้างาน | `foreman.html` |
| **`แผนงาน`** หรือ **`วางแผน`** / **`gantt`** | 📋 **เปิดระบบวางแผนงานประจำเดือน (ผู้รับเหมา - LV2)**: วางแผนแบบ Gantt Chart 28-31 วัน, ลากแถบกราฟปรับวันที่, เพิ่มงานย่อยแบบ Checklist และส่งขออนุมัติ PM | `weekly-plan.html` |
| **`PM`** หรือ **`อนุมัติ`** / **`แดชบอร์ด`** | 👔 **เปิดศูนย์บริหารโครงการ (PM - LV3)**: ตรวจสอบและอนุมัติแผนงาน, ดูแผนภูมิ Master Gantt รวมทุกผู้รับเหมา และสืบค้นคลังรายงานย้อนหลัง | `pm-center.html` |
| **`โปรไฟล์`** หรือ **`ข้อมูลฉัน`** | 👤 **ตรวจสอบข้อมูลผู้ใช้**: ตรวจสอบชื่อ, ตำแหน่ง, เลเวล, บริษัท, โครงการที่สังกัด และ LINE UID ที่บันทึกไว้ในชีต `Site_Users` | ตามเลเวลผู้ใช้ |

> 💡 **แถบ Quick Reply**: ทุกครั้งที่มีการตอบกลับ บอทจะมีปุ่มลัดปรากฏขึ้นเหนือช่องพิมพ์ข้อความ (เช่น `📱 รายงานหน้างาน`, `📋 แผนงาน Gantt`, `👔 ศูนย์ PM`, `👤 โปรไฟล์ของฉัน`, `❓ ดูคำสั่งทั้งหมด`) สามารถแตะส่งได้โดยไม่ต้องพิมพ์ข้อความเอง
